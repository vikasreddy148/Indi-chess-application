server:
  port: 8082

spring:
  application:
    name: match-service
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/match_service_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:Vikky@148}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

# Feign Client Configuration
app:
  jwt:
    secret: ${JWT_SECRET:change-me-change-me-change-me-change-me-32chars-min}

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
  user-service:
    url: ${USER_SERVICE_URL:http://user-service:8081}

logging:
  level:
    root: INFO
    com.indichess.match: DEBUG
    org.springframework.web.socket: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
